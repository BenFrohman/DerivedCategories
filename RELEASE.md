# Priority snapshot — 23 September 2026

Branch: `priority-2026-09-23` at `965e93356c27be70bf1efb0c0043965e68e358c9`.
This is a freeze point for the residual-surface pass. It is not a GitHub Release tag; the connector cannot mint `git tag` or Releases.

## Locked claim

Residual quintic surfaces on `X = V(F)` with `F = x0^5 x3 + x3^6 + x1^5 x4 + x4^6 + x2^5 x5 + x5^6` and plane `Π = V(x3,x4,x5)`.

Not Hodge. Not a Clay counterexample. Not a fourfold partner `Y`.

## Rulesets still active (must be toggled in the GitHub UI)

- HODGE `owner-approval-main` id `23858506` — https://github.com/BenFrohman/HODGE/rules/23858506
- NS_Millennium_Proof `SUPACOOPA` id `22587693` — https://github.com/BenFrohman/NS_Millennium_Proof/rules/22587693

Set Enforcement to Disabled on those two pages. The API available here can list and create rulesets; it cannot update or delete the existing ones.
